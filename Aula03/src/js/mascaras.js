/**
 * Utilizaremos a variavel global do jquery (S) para carregar a mascara de telefone assim que a pagina for carregada.
 */

$(document).ready(function (){
    // Implementa a Mascara de Telefone
    $('#telefone').mask('(00) 00000-0000');

 });